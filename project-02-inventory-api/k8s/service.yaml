apiVersion: v1
kind: Service
metadata:
  name: inventory-api
  namespace: inventory

spec:
  selector:
    app: inventory-api

  ports:
    - protocol: TCP
      port: 8000
      targetPort: 8000

  type: ClusterIP
